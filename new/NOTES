file structure:
* participants
    * megahomyak
        * pfp
        * links (text file, each link on new line, no more data in there, display links as raw URLs (that is, content and href must match))
        * GENERATED index.html (with links)
* discussions
    * nosey_foxes
        * contents (this is where the markup goes)
        * ...maybe other files here, like images...
        * GENERATED index.html (with contents and other files)
* COPIED index.html (copy of "page_{N}.html" with greatest "N")
* GENERATED page_{N}.html (pages of index to have pagination)

PAGE STRUCTURE:
participant:
    body (centered items) {
        heading { [participant name] }
        image { [participant pfp] }
        bullet list (list) {
            link { [link] }
        }
    }

index:
    body (centered items) {
        heading { "..." }
        subtitle { "..." }
        div discussion (repeating) {
            title (link) {
                emoji span {}
                text span {}
            }
            moment {}
            "participants" (p) {
                "Participants: "
                a (participant url) {}
                ", "
                a (participant url) {}
                ", "
                a (participant url) {}
                ", "
                ...
            }
        }
        navigation {
            link { "prev" }
            link { "next" }
        }
    }

discussion:
    body (not centered) {
        title (centered) {}
        message (repeating; horizontal container) {
            pfp {}
            content (vertical container) {
                metadata (horizontal container) {
                    username span {}
                    moment span {}
                    reply to id span (if present) {}
                    id span (if present) {}
                }
                parts (vertical container) {
                    SWITCH part {
                        image => img,
                        text => <pre>text</pre>,
                    }
                }
            }
        }
    }
